# 今日作业提交内容

## 作业一：核心页面截图

请按以下顺序上传 3 张图片，并在每张图片下方粘贴对应说明。

### 截图 1：需求输入与 AI 连接状态

上传文件：`docs/screenshots/01_input_ai_connected.png`

配文：

> 图 1：智能导购需求输入页。页面显示 AI 服务已连接，用户既可以输入自然语言需求，也可以补充预算、最低内存、使用场景、重量和显卡等结构化条件。表单中的明确条件优先于模型推断，“希望轻便”等模糊表达只作为软偏好，不会被错误转换为硬约束。

### 截图 2：normal 模式推荐结果

上传文件：`docs/screenshots/02_normal_recommendation.png`

配文：

> 图 2：normal 模式的真实 AI 推荐结果。千问模型先提取结构化需求，确定性搜索引擎再完成硬约束过滤、场景评分、排序与品牌多样性重排。页面展示固定商品排名、关键参数、匹配需求、取舍说明和 fact/evidence 证据，模型只解释结果，不能自行新增商品或修改排名。

### 截图 3：nearest 模式冲突反馈

上传文件：`docs/screenshots/03_nearest_conflict.png`

配文：

> 图 3：冲突约束下的 nearest 模式。当预算、重量和独显要求无法同时满足时，系统不会伪造完全匹配结果，而是返回最多 2 款最接近候选，明确标记“非完全符合”，并列出违反的约束、冲突幅度及可放宽建议，形成从需求输入到异常反馈的完整体验闭环。

截图区域最后可补充：

> 在线体验地址：https://laptop-shopping-agent-ku1u.vercel.app

## 作业二：在线部署与产品文档

可直接提交以下内容：

### 项目简介与产品定位

智选本是一款面向笔记本电脑品类的 AI 智能导购应用。用户可以用自然语言和结构化表单描述预算、性能、重量、内存及使用场景，系统输出可解释的商品推荐、取舍分析和证据来源。产品采用“模型理解 + 规则决策”的混合架构：阿里云百炼千问负责需求理解和推荐解释，确定性搜索引擎负责硬约束过滤、评分、排序及冲突分析，避免大模型随意决定商品名单。当前 10 款商品与 30 条文档均为教学用 mock 数据，不代表实时市场信息。

### 在线链接

- 在线应用：https://laptop-shopping-agent-ku1u.vercel.app
- GitHub 与完整 README：https://github.com/DavidLYJ111/laptop-shopping-agent

### 启动方式与环境变量

运行环境为 Python 3.10 及以上版本。在项目根目录创建虚拟环境并安装依赖：

```powershell
py -3.12 -m venv .venv
.venv\Scripts\python -m pip install -e ".[dev]"
Copy-Item .env.example .env
```

在 `.env` 中配置：

```dotenv
BAILIAN_API_KEY=你的阿里云百炼密钥
AI_MODEL=qwen-plus
BAILIAN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
```

启动命令：

```powershell
.venv\Scripts\python -m uvicorn shopping_agent.api.main:app --reload
```

访问 `http://127.0.0.1:8000` 即可使用。线上版本通过 GitHub 与 Vercel 自动部署，密钥保存在 Vercel Environment Variables 中，不会提交到仓库。

### 核心使用流程与 AI 介入环节

1. 用户输入自然语言需求，也可填写预算、内存、场景和重量等表单条件。
2. 第一次千问调用通过 JSON Mode 提取意图、硬约束和软偏好，并由 Pydantic 校验。
3. 表单中的明确条件覆盖模型推断结果。
4. `search_products` 执行硬约束过滤、场景评分、品牌多样性重排和 Top-K 输出。
5. 无完全匹配商品时进入 nearest 模式，返回最多 2 个候选并列出违约项。
6. 本地检索只允许 fact 和 evidence 文档作为可信依据，derived 不作为事实校验依据。
7. 第二次千问调用仅为固定候选生成推荐解释，不允许更改 SKU 和排名。
8. 服务端再次校验 SKU、排名、证据 ID 和 normal/nearest 语义后返回结果。

AI 只介入“结构化需求理解”和“有证据的推荐解释”两个环节；商品过滤、候选名单、排名、违约数量及幅度全部由确定性 Python 代码计算。

### 技术栈清单

- 前端：单文件 HTML、CSS、JavaScript、Fetch API
- 后端：Python、FastAPI、Uvicorn
- 大模型：阿里云百炼通义千问 `qwen-plus`
- 模型协议：OpenAI 兼容 Chat Completions、JSON Mode
- 数据与校验：Pydantic、JSONL
- 检索：本地关键词匹配，仅使用 fact/evidence
- 测试：pytest、FastAPI TestClient，共 33 项测试
- 版本管理与部署：Git、GitHub、Vercel

